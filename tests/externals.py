class glob:  # noqa
    # UT{ glob_glob
    @staticmethod
    def glob(path):
        # UT= assert type(path) == str
        # UT= print('glob.glob(%s)' % path)
        pass
    # UT}

class os:  # noqa
    class path:  # noqa

        # UT{ os_path_isfile
        @staticmethod
        def isfile(path):
            # UT= assert type(path) == str
            # UT= print('os.path.isfile(%s)' % path)
            pass
        # UT}
